%li
  =link_to user do
    .thumbnail
      = image_tag user.avatar.url(:thumb)
    .meta
      %h3
        = user
      .description
        = truncate strip_tags(user.about), :length => 250
    .cl
