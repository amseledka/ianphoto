.invites
  %h2 Список электронных адресов для приглашения
  %ul
    - @invites.each do |invite|
      %li
        = invite.email
        - if invite.invited?
          Invited
        - else
          = link_to 'Послать приглашение', send_invitation_path(invite), :confirm => 'Послать приглашение? ', :method => :post
        = link_to 'Destroy', invite, :confirm => 'Вы уверены?', :method => :delete

  = link_to 'Добавить электронный адрес для приглашения', new_invite_path