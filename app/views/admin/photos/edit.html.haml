- content_for :css do
  = stylesheet_link_tag "admin"

- @page_title = "Редактирование фотографии"
#content
  .photo_form
    %h2 
      Редактирование фотографии
      -if @category
        %small (или вернуться в #{link_to @category, admin_category_path(@category)})
    =render "form"
